/// Configurações globais do aplicativo.
class AppConfig {
  /// Stream Chat API Key (pública, client-side).
  /// A chave secreta fica apenas no backend.
  static const String streamChatApiKey = 'e78m62ehrtr2';
}
